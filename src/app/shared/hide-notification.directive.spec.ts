import { HideNotificationDirective } from './hide-notification.directive';

describe('HideNotificationDirective', () => {
  it('should create an instance', () => {
    const directive = new HideNotificationDirective();
    expect(directive).toBeTruthy();
  });
});
